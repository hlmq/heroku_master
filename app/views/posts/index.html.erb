<h1>Listing posts</h1>
<%= link_to "Homepage", :controller => "welcome", :action => "index" %>
<br/>
<%= link_to "New post", new_post_path %>

<table>
  <tr>
    <th>Id</th>
    <th>Title</th>
    <th>Text</th>
  </tr>

  <% @posts.each do |post| %>
      <tr>
        <td><%= post.id %></td>
        <td><%= post.title %></td>
        <td><%= post.text %></td>
        <%
           # to link to other views, we can use
           #    1. link_to "Text", :controller => "posts", :action => "show", :id => post.id
           #    2. Open cmd, cd to rails app, type rake routes and use alias name of each route.
           #        All route must suffix with _path. Ex: link_to "Text", edit_post_path
           #        For DELETE, PUT or params need on the route, we can type like this: link_to "Edit", edit_post_path(post)
        %>
        <td><%= link_to "Show", :controller => "posts", :action => "show", :id => post.id %></td>
        <td><%= link_to "Edit", edit_post_path(post.id) %></td>
        <td><%= link_to "Delete", post_path(post), method: :delete, data: {confirm: "Are you sure?" }  %></td>
      </tr>
  <% end %>
</table>
